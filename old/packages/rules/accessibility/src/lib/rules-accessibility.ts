import { Page } from "puppeteer";

export function rulesAccessibility(): string {
  return 'rules-accessibility';
}

export function generateAccessibilityTree(page: Page, url: string): any {
  return '';
}